import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Add output export for static site generation (SSG) deployment option
  output: 'export',
};

export default nextConfig;